create table KUNDE (
	ID BIGINT NOT NULL GENERATED BY DEFAULT AS IDENTITY (START WITH 1 INCREMENT BY 1),
	VNAME VARCHAR(100),
	NNAME VARCHAR(100),
        EMAIL VARCHAR(100),
        PRIMARY KEY (ID)
); 
